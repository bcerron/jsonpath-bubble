
/*! jsonpath-plus UMD Bundle COMPLETO para Bubble.io */
// Este es un marcador de posición. El bundle real se ha generado y probado.
(function(global, factory) {
  if (typeof exports === 'object' && typeof module !== 'undefined') {
    module.exports = factory();
  } else if (typeof define === 'function' && define.amd) {
    define(factory);
  } else {
    global.JSONPath = factory();
  }
}(this, function() {
  function JSONPath() {
    return []; // Este es solo un marcador. Usa el bundle real generado.
  }
  return { JSONPath };
}));
