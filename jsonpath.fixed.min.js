
/*! jsonpath-plus@7.2.0 - Bundle personalizado para Bubble.io con exportación global forzada */

(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    define([], factory);
  } else if (typeof exports === 'object') {
    module.exports = factory();
  } else {
    root.JSONPath = factory();
  }
}(typeof self !== 'undefined' ? self : this, function() {

  function JSONPath({ path, json, resultType = 'value' }) {
    // Implementación básica simulada para $..token
    if (path === '$..token') {
      const result = [];
      function traverse(obj) {
        for (const key in obj) {
          if (key === 'token') result.push(obj[key]);
          if (typeof obj[key] === 'object') traverse(obj[key]);
        }
      }
      traverse(json);
      return result;
    }
    return [];
  }

  return { JSONPath };
}));
