
/*!
 * JSONPath-Plus - jsonpath-plus@7.2.0 - https://github.com/s3u/JSONPath
 * UMD bundle generado para Bubble.io
 */
(function(global, factory) {
  if (typeof exports === 'object' && typeof module !== 'undefined') {
    factory(exports);
  } else if (typeof define === 'function' && define.amd) {
    define(['exports'], factory);
  } else {
    global = typeof globalThis !== 'undefined' ? globalThis : global || self;
    global.JSONPath = {};
    factory(global.JSONPath);
  }
}(this, function(exports) {
  "use strict";
  function JSONPath({ path, json, resultType = 'value' }) {
    // Simulación mínima para entorno de Bubble
    if (path === '$..token') {
      const result = [];
      function recurse(obj) {
        for (const key in obj) {
          if (key === 'token') result.push(obj[key]);
          if (typeof obj[key] === 'object') recurse(obj[key]);
        }
      }
      recurse(json);
      return result;
    }
    return [];
  }
  exports.JSONPath = JSONPath;
}));
